# Stage.py
# @doc docs/python/custom_ui_doc/Stage.rst
# @export_name stage
# 本文件由自动脚本生成，用于提供 linting/补全，真实逻辑请在原生环境实现

def add_widget(widget_obj) -> None:
    """将参数中的控件添加到 UI 界面中

    Args:
        widget_obj (object): 需要添加进 UI 界面的控件对象

    Returns:
        None: 无

    Example:
        >>> stage.add_widget(widget_obj)
    """
    return None

def remove_widget(widget_obj) -> None:
    """从 UI 界面移除参数传入的控件

    Args:
        widget_obj (object): 需要从 UI 界面移除的控件

    Returns:
        None: 无

    Example:
        >>> stage.remove_widget(my_button)
        # 从 UI 界面中移除控件 my_button
    """
    return None
